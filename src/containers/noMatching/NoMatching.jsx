
import React from 'react';
import PropTypes from 'prop-types';
import styles from './noMatching.scss';
const NoMatching = ({}) => {

    return (
        <div>"NoMatching PLACEHOLDER"</div>
    );
};

NoMatching.propTypes = {
};

NoMatching.defaultProps = {
};

export default NoMatching;