
import React from 'react';
import PropTypes from 'prop-types';
import styles from './home.scss';
const Home = ({}) => {

    return (
        <div>"testing"</div>
    );
};

Home.propTypes = {
};

Home.defaultProps = {
};

export default Home;